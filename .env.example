# ============================================================
# AgriFlow Environment Variables — EXAMPLE (safe to commit)
# ============================================================
# Copy this file to .env and fill in real values for LOCAL DEV.
# For PRODUCTION: paste these into your hosting platform's
# Secrets/Environment Variables dashboard (HF Spaces, Railway, etc.)
# NEVER paste API keys into netlify.toml or any committed file.
# ============================================================

# ── Archia Platform ──────────────────────────────────────────
# Get from: console.archia.app → your project → API Keys
ARCHIA_TOKEN=

# ── USDA NASS Quick Stats ────────────────────────────────────
# Get from: quickstats.nass.usda.gov → API → Request Key
NASS_API_KEY=

# ── OpenRouteService (optional) ─────────────────────────────
# Get from: openrouteservice.org → free signup → Dashboard → Tokens
# Without this key, route optimizer uses straight-line estimates (still works)
OPENROUTESERVICE_API_KEY=

# ── Model routing ────────────────────────────────────────────
DEFAULT_MODEL=priv-claude-sonnet-4-5-20250929
PLANNER_MODEL=agent:agriflow-planner
FAST_MODEL=priv-claude-3-5-haiku-20241022
DATA_MODEL=priv-claude-3-5-haiku-20241022
VIZ_MODEL=priv-claude-3-5-haiku-20241022
LOGISTICS_MODEL=priv-claude-3-5-haiku-20241022
ML_MODEL=priv-claude-sonnet-4-5-20250929
SQL_MODEL=priv-claude-sonnet-4-5-20250929
ANALYST_MODEL=priv-claude-sonnet-4-5-20250929
SYNTHESIZER_MODEL=priv-claude-sonnet-4-5-20250929

# ── Database ─────────────────────────────────────────────────
DB_PATH=data/agriflow.db

# ── Server (local dev only — hosting platform sets PORT) ─────
API_HOST=0.0.0.0
API_PORT=8000

# ── Frontend (set in Netlify dashboard only — NOT a secret) ──
# This is the public URL of your HF Spaces backend.
# VITE_API_URL=https://YOUR-USERNAME-agriflow.hf.space
